_schema-version: "3.2"
ID: nryzyemployees
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nryzyemployees-destination-content
  type: com.sap.application.content
  requires:
  - name: nryzyemployees-destination-service
    parameters:
      content-target: true
  - name: nryzyemployees-repo-host
    parameters:
      service-key:
        name: nryzyemployees-repo-host-key
  - name: nryzyemployees-uaa
    parameters:
      service-key:
        name: nryzyemployees-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: nryzyemployees_html_repo_host
          ServiceInstanceName: nryzyemployees-html5-srv
          ServiceKeyName: nryzyemployees-repo-host-key
          sap.cloud.service: nryzyemployees
        - Authentication: OAuth2UserTokenExchange
          Name: nryzyemployees_uaa
          ServiceInstanceName: nryzyemployees-xsuaa-srv
          ServiceKeyName: nryzyemployees-uaa-key
          sap.cloud.service: nryzyemployees
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: nryzyemployees-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nryzyemployees-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nryzyemployees.zip
      name: nryzyemployees
      target-path: resources/
- name: nryzyemployees
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nryzyemployees-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nryzyemployees-destination-service
    service-plan: lite
- name: nryzyemployees-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: nryzyemployees-xsuaa-srv
    service-plan: application
- name: nryzyemployees-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nryzyemployees-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
