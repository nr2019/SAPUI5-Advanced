_schema-version: "3.2"
ID: nryzylists
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nryzylists-destination-content
  type: com.sap.application.content
  requires:
  - name: nryzylists-destination-service
    parameters:
      content-target: true
  - name: nryzylists-repo-host
    parameters:
      service-key:
        name: nryzylists-repo-host-key
  - name: nryzylists-uaa
    parameters:
      service-key:
        name: nryzylists-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: nryzylists_html_repo_host
          ServiceInstanceName: nryzylists-html5-srv
          ServiceKeyName: nryzylists-repo-host-key
          sap.cloud.service: nryzylists
        - Authentication: OAuth2UserTokenExchange
          Name: nryzylists_uaa
          ServiceInstanceName: nryzylists-xsuaa-srv
          ServiceKeyName: nryzylists-uaa-key
          sap.cloud.service: nryzylists
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: nryzylists-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nryzylists-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nryzylists.zip
      name: nryzylists
      target-path: resources/
- name: nryzylists
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nryzylists-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nryzylists-destination-service
    service-plan: lite
- name: nryzylists-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: nryzylists-xsuaa-srv
    service-plan: application
- name: nryzylists-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nryzylists-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
